# Freestanding Guest Networking Architecture

## Goal

The project keeps its KVM/vhost-net Host backend and implements the Guest
frontend freestanding. The Guest owns a small Virtio-net driver and the minimum
Ethernet, ARP, IPv4, and UDP functionality needed for UDP Echo and public DNS
query demos. The existing TAP setup scripts configure the Host as the Guest's
gateway and apply forwarding/NAT toward an uplink. vhost-net owns the Virtio
data path.

## Packet path

```text
testN1/guest.c or dns_guest.c
  UDP Echo validation or public DNS A-record query
       │
src/guest/network_stack.c
  UDP ── IPv4 ── ARP ── Ethernet II
       │ Ethernet frames
src/guest/virtio_net.c
  Virtio-MMIO v2; split RX queue 0 and TX queue 1
       │ shared Guest-RAM rings/buffers + QUEUE_NOTIFY
KVM_IOEVENTFD ── vhost-net ── TAP (vmtapN)
       │
Host kernel: TAP address + IP forwarding + MASQUERADE
       │
Host uplink ── configured UDP Echo peer or 1.1.1.1 DNS resolver
```

The Host continues to own the Virtio-MMIO register model, vhost lifecycle,
TAP fd, and KVM eventfd wiring. Guest queue notifications are delivered to
vhost-net without a userspace per-packet ring parser. The Host also installs
a small GDT so the Guest's IDT code selector resolves during IRQ delivery.
The vhost backend and TAP/NAT scripts retain their existing packet path.

## Guest responsibilities

- `virtio_net.c` negotiates `VERSION_1` and `NET_F_MAC`, reads the device MAC,
  and configures RX queue 0 and TX queue 1 with split rings in Guest DMA memory.
- TX copies one Ethernet frame into a Virtio buffer and publishes it to the
  available ring. RX consumes the used ring, strips the 10-byte Virtio-net
  header, copies the frame to the protocol layer, and recycles the descriptor.
- RX enables used-buffer interrupts. The Guest installs an IDT gate for PIC
  IRQ 5, acknowledges Virtio-MMIO interrupts, and sleeps with HLT while
  waiting for the UDP reply. TX completion remains polled.
- `network_stack.c` builds/parses Ethernet II, answers ARP for the Guest IP,
  learns the TAP gateway MAC, creates/validates basic IPv4 headers, and sends
  or receives UDP datagrams. Outbound IP traffic uses the configured gateway.
- `testN1/guest.c` sends a fixed payload to a configurable UDP Echo endpoint
  and verifies the response source, source port, and exact payload.
- `testN1/dns_guest.c` asks `1.1.1.1` for the A record of `example.com` and
  checks the returned DNS answer before printing its IPv4 address.

The Guest has no libc, scheduler, sockets, DHCP, TCP, general ARP cache,
fragment reassembly, or IPv4 routing table. Its static `10.200.<vm+1>.2/24`
address and `10.200.<vm+1>.1` gateway match `scripts/net-setup.sh`.

## Echo and public DNS validation

The default Guest destination is the TAP gateway on UDP port 9999. The VMM
does not include a UDP Echo service, so a separate service must be available at
that endpoint for the local TAP test. For an end-to-end NAT test, build the
Guest with `UDP_ECHO_IP` and `UDP_ECHO_PORT` set to an external, reachable UDP
Echo service. Then start the VMM with `--net`, run `scripts/net-setup.sh` for
the VM and uplink, and observe the Guest's success message after it validates
the returned peer and payload.

`testN1/dns_guest.c` provides a public endpoint without requiring a separate
Echo service. Build it with `make testN1_dns`, run `bin/testN1_dns.img` with
`--net`, and configure the Host NAT in the same way. By default, a successful
response prints the A record returned for `example.com` by `1.1.1.1`.

ARP retry is intentional: the Guest can begin running before the TAP interface
has been assigned its gateway address by the setup script.

## Current scope and limitations

Each demo is a single-vCPU x86-64 freestanding test image using one fixed
Virtio-MMIO network device. The transport uses one fixed DMA arena and queue
sizes up to eight entries. The protocol layer supports untagged Ethernet II,
basic ARP, IPv4 with header checksum validation, and UDP; it does not implement
DHCP, ICMP, TCP, VLANs, IP options beyond skipping their header length,
fragmentation, or multiple pending UDP datagrams.

The VMM routes vhost RX completion notifications through KVM IRQFD to PIC IRQ 5.
The Guest remaps the PIC, installs the corresponding IDT gate, and enables
interrupts after Virtio-net initialization.


可以。整个过程大致是“先判断卡在哪一段，再逐层排除，最后找到协议头长度不一致”。

### 1. 先判断 Guest 卡在哪里

最初只看到 `Public DNS query demo started`，这只能说明 Guest 程序启动了，并不代表网卡已经初始化或发出了网络包。

后来增加了阶段日志，看到：

```text
Virtio-net initialized; resolving TAP gateway ARP
```

这说明 VirtIO-net 初始化成功，但还没拿到网关 MAC。DNS 测试必须先通过 ARP 找到 `10.200.1.1`，所以此时它还没真正开始 DNS 查询。只有出现 `DNS query ... sent`，才说明 DNS 请求已排入网卡发送队列。

相关等待逻辑在 [network_stack.c](</home/bbm/workspace/System Dev/VMM/src/guest/network_stack.c:27>)，阶段日志在 [dns_guest.c](</home/bbm/workspace/System Dev/VMM/test/testN1/dns_guest.c:153>)。

### 2. 先排查 TAP、路由和 NAT

你贴出的网络配置显示：

- Guest 地址是 `10.200.1.2`
- TAP 网关地址是 `10.200.1.1/24`
- NAT 和转发规则已设置，出口是 `ppp1`

因此配置方向看起来是对的，暂时没有必要继续改防火墙规则。

当时我还看到 VMM vCPU 占用接近 100%。这和 Guest 在循环轮询 ARP/RX 队列相符，但本身不能说明具体是哪一层坏了。

### 3. 排查队列通知时，没有把“没看到用户态日志”当成结论

我给用户态 `QUEUE_NOTIFY` 路径加了一个 fallback 和日志。但 VirtIO 队列通知通常会被 `KVM_IOEVENTFD` 直接转给 eventfd，再由 vhost 处理，不一定进入用户态 MMIO 处理。因此，fallback 日志没有出现，**不能证明 Guest 没有 kick 队列**。

与此同时，TAP 接口的计数在增长。这说明数据路径有活动，问题不像是简单的“Guest 完全没有通知后端”。

### 4. 找到关键问题：现代 VirtIO-net 头长度不一致

Guest 协商了 `VIRTIO_F_VERSION_1`，但项目原来把 VirtIO-net header 当成 **10 字节**：

- Guest TX/RX 都按 10 字节放置或跳过 header。
- Host 创建 TAP 时也通过 `TUNSETVNETHDRSZ` 把 header 长度设成了 10 字节。

而 Linux vhost-net 对现代 `VERSION_1` 模式使用 **12 字节**的 VirtIO-net header。这样 Guest、TAP 与 vhost 对“以太网帧从哪里开始”的理解不一致；尤其是 RX 方向，Guest 可能把 header 的尾部当作以太网帧内容，无法正确识别网关 ARP 回复。Linux vhost-net 的实现会根据 `VIRTIO_F_VERSION_1` 选择扩展 header；规范也描述了现代网络设备的 header 格式。[Linux vhost-net](https://github.com/torvalds/linux/blob/master/drivers/vhost/net.c)、[OASIS VirtIO 规范](https://docs.oasis-open.org/virtio/virtio/v1.0/virtio-v1.0.html)

我把 12 字节定义集中到 [virtio_defs.h](</home/bbm/workspace/System Dev/VMM/inc/virtio_defs.h:31>)，并让 Guest 和 TAP 共用它：[Guest 网卡驱动](</home/bbm/workspace/System Dev/VMM/src/guest/virtio_net.c:281>)、[TAP 初始化](</home/bbm/workspace/System Dev/VMM/src/host/tap_device.cpp:37>)。

### 5. 重建并用更强的结果验证

`make -B all` 通过，网络栈单元测试也通过。重启后，你看到：

```text
DNS query for example.com sent to resolver
Public DNS reply: example.com = 104.20.23.154
```

第二行最关键：测试不只是把数据排入 TX 队列，而是收到了并验证了匹配 `example.com` 查询的 DNS 答复。因此，Guest VirtIO-net、vhost、TAP、Host NAT 和公网 DNS 往返都已跑通。

另外，我曾临时把 ARP 重试间隔调短来辅助观察，发现这会造成过于频繁的 ARP 重发，所以在最终构建前恢复了原来的较低重试频率；这不是最终修复。抓包窗口里的几条 ARP 请求是建链阶段的请求，不能推翻 Guest 后续收到 DNS 回复这一结果。